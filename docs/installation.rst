============
Installation
============

At the command line::

    $ easy_install django-mlcommenting


Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mlcommenting
    $ pip install django-mlcommenting

